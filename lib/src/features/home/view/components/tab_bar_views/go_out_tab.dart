import 'package:flutter/material.dart';

class GoOutTab extends StatelessWidget {
  const GoOutTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Sair',
      ),
    );
  }
}
